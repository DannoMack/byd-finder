# Cloudflare Pages redirects
# Canonical domain: bydfinder.com
# bydfinder.ca and bydfinder.org are custom domains that 301 to canonical.
# These rules are enforced by per-domain Pages custom-domain routing; this file
# handles path-level redirects on whichever domain is serving.

# Canada-first: root goes to /ca/
/          /ca  302

# Preorder page superseded by dealers page
/ca/preorder  /ca/dealers  301
/ca/preorder/ /ca/dealers  301
